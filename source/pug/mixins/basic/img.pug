mixin img(path, image, format, width, height, altText, className, loading)
  img(src="img/" + path + "/" + image + "@1x." + format, srcset= "img/" + path + "/" + image + "@2x." + format + " 2x", width= width, height= height, alt= altText, class= className, loading= loading)
